¡Perfecto! Incorporar la pila tecnológica específica y la estrategia de la base de datos es clave para que la documentación sea un reflejo fiel del proyecto.

Aquí tienes la versión definitiva del documento, integrando que el backend es **Node.js con Express**, la base de datos es **MySQL** y se gestiona con un **único script de creación**.

***

## Documentación Maestra: Sistema de Gestión POS

Este documento es la "fuente de la verdad" para el desarrollo y la reestructuración del sistema de punto de venta. Describe un sistema **consistente, seguro, escalable y multi-tenant**, donde toda la lógica de negocio reside en el backend.

---

### **Pila Tecnológica y Estrategia de Despliegue** 🛠️

* **Backend:** El motor del sistema está construido sobre **Node.js** utilizando el framework **Express.js**. Esta elección permite un desarrollo ágil y un alto rendimiento para las operaciones de la API.
* **Base de Datos:** Toda la información se almacena en una base de datos relacional **MySQL**.
* **Estrategia de Base de Datos:** Para simplificar el desarrollo y garantizar un entorno limpio y predecible, el sistema no utiliza migraciones incrementales. En su lugar, se emplea un **único esquema de creación**. La base de datos completa se genera desde cero ejecutando un solo comando (`npm run db:create`), lo que asegura que todos los entornos de desarrollo partan de una estructura idéntica y actualizada.

---

### **Principios Fundamentales del Sistema** 🏛️

Para que el sistema funcione de manera robusta y segura, se basa en cuatro pilares esenciales:

* **Lógica Centralizada en el Backend:** Toda la "inteligencia" del sistema reside en el servidor de **Express.js**. Acciones como calcular el stock después de una venta o crear una nueva empresa al registrarse no ocurren en la base de datos, sino en un núcleo centralizado. Esto hace que el sistema sea más seguro, fácil de actualizar y predecible.
* **Nada se Borra Realmente (Soft Deletes):** Cuando un usuario "elimina" algo, como un producto o un cliente, el sistema no lo borra de forma permanente. Simplemente lo marca como "eliminado" y lo oculta de la vista. Esto es crucial para mantener la integridad de los reportes históricos y permite recuperar información si es necesario.
* **Aislamiento Total de Datos (Multi-Tenancy):** El sistema está diseñado para que múltiples empresas puedan usarlo simultáneamente sin que ninguna pueda ver la información de otra. Cada dato, desde un producto hasta una venta, está estrictamente asociado a una única empresa. Es como si cada negocio operara en su propia burbuja privada dentro de la misma aplicación.
* **Nomenclatura Consistente:** Todos los elementos técnicos del sistema siguen un estándar de nombres en español y un formato predecible. Esto, aunque es un detalle interno, se traduce en un desarrollo más rápido y menos propenso a errores, lo que garantiza un sistema más estable para el usuario final.

---

### **Flujos de Negocio Críticos**

Estos son los procesos clave que demuestran cómo funciona el sistema en momentos cruciales.

#### **Flujo 1: El Nacimiento de un Nuevo Negocio en el Sistema**

Este es el proceso que ocurre cuando un nuevo dueño de negocio (un **Superadmin**) se registra por primera vez, típicamente usando su cuenta de Google.

1.  **Registro y Verificación:** El usuario se autentica con Google. El backend de Express recibe sus datos básicos (nombre, email) y comprueba en la base de datos MySQL que no exista previamente.
2.  **Creación Atómica del Entorno:** Al ser un usuario nuevo, el sistema inicia un proceso de "creación de entorno" que es **transaccional**, lo que significa que todos los pasos deben completarse con éxito. Si uno falla, todo se deshace para evitar inconsistencias.
    * Primero, crea el registro del **usuario** y le asigna el rol de **Superadmin**.
    * Inmediatamente, crea una **empresa** a su nombre (ej. "La Empresa de Juan Pérez").
    * Luego, para esa empresa, genera automáticamente una **sucursal** por defecto ("Sucursal Principal").
    * Dentro de esa sucursal, crea un **almacén** ("Almacén Principal") y una **caja registradora** ("Caja 1").
    * Finalmente, crea entidades básicas para que el negocio pueda operar desde el minuto uno: un "Cliente Genérico", métodos de pago como "Efectivo" y "Tarjeta", y una categoría de productos "General".
3.  **Acceso Inmediato:** Una vez que todo este ecosistema se ha creado, el usuario es automáticamente logueado y ya puede empezar a configurar su negocio.

---

### **Modelo de Seguridad y Acceso a Datos** 🔐

El sistema garantiza que cada usuario vea únicamente lo que le corresponde mediante un **middleware de seguridad en Express.js**, que actúa como un guardia en la puerta de entrada a los datos.

1.  **Credencial de Acceso (Token):** Cuando un usuario inicia sesión, recibe una credencial digital (un token JWT) que es como su pase de acceso personal.
2.  **Verificación en Cada Petición:** Cada vez que el usuario intenta hacer algo (como ver productos o registrar una venta), presenta esta credencial en la cabecera de la petición a la API.
3.  **El Guardia (Middleware):** El backend de Express intercepta la petición, revisa la validez de la credencial y, lo más importante, consulta quién es el usuario y a qué **empresa** pertenece.
4.  **Contexto Seguro:** Con esta información, el sistema adjunta una etiqueta invisible a la acción del usuario que dice: "Esta persona pertenece a la Empresa X". A partir de ese momento, cualquier consulta a la base de datos MySQL llevará automáticamente el filtro "sólo para la Empresa X", garantizando el aislamiento total de los datos.

---

### **Casos de Uso por Rol de Usuario**

Aquí se describe qué hace cada tipo de persona dentro del sistema.

#### **👑 Superadmin (Dueño del Negocio)**

El Superadmin tiene el control total sobre la configuración y supervisión de su negocio.

* **Configuración Inicial:** Tras el registro, su primera tarea es personalizar el sistema: sube el logo de su empresa, define el impuesto local (ej. IVA), la moneda, y el texto que aparecerá al pie de los tickets de venta.
* **Estructura del Negocio:** Si abre nuevos locales, crea más **sucursales**. Dentro de cada una, define sus **almacenes** y **cajas registradoras**.
* **Gestión de Catálogo:** Es el responsable de crear las **categorías** de productos y de dar de alta cada **producto**, estableciendo su precio de venta, costo, y si se controlará su inventario.
* **Gestión de Personal:** Cuando contrata a un empleado, crea un nuevo **usuario** en el sistema, le asigna un rol (como "Cajero") y lo vincula a una sucursal y caja específicas. También decide a qué partes del sistema puede acceder ese empleado.
* **Supervisión y Análisis:** Su día a día implica revisar el **Dashboard** para ver las ventas totales, las ganancias, los productos más vendidos y el flujo de efectivo. Puede generar reportes para saber qué productos están por agotarse y revisar los cierres de caja de sus empleados para controlar el dinero.

#### **👤 Empleado (Cajero / Vendedor)**

El empleado tiene un rol operativo, centrado casi exclusivamente en el punto de venta.

* **Inicio y Cierre de Turno:** Su jornada comienza iniciando sesión. Realiza la **"Apertura de Caja"**, donde registra la cantidad de dinero base con la que empieza. Al finalizar, hace el **"Cierre de Caja"**, donde el sistema le informa cuánto dinero debería haber según las ventas, y él introduce cuánto dinero contó físicamente para registrar cualquier diferencia.
* **Proceso de Venta:** Es su tarea principal. Utiliza un lector de código de barras o busca productos por nombre para añadirlos al carrito de compra. Si el cliente no quiere un producto, lo quita, y el sistema automáticamente devuelve esa unidad al inventario. Procesa el pago eligiendo el método (efectivo, tarjeta), y el sistema calcula el vuelto si es necesario. Al confirmar, la venta se registra, el stock se descuenta y se imprime el ticket.
* **Movimientos de Caja:** Si necesita usar dinero de la caja para algo que no es una venta (como pagarle a un repartidor), registra un **"egreso"** para que el dinero esté justificado al final del día.

#### **🛍️ Usuario Normal (Cliente)**

El cliente es la razón de ser del negocio, pero no interactúa directamente con el software.

* **Realizar una Compra:** Elige sus productos, se acerca a la caja y realiza el pago. Si desea una factura con sus datos, se los proporciona al cajero, quien lo puede registrar en el sistema como un nuevo cliente. Si no, la venta se asocia a un "Cliente Genérico". Finalmente, recibe su ticket impreso.
* **Solicitar una Devolución:** Si necesita devolver un producto, presenta su ticket. El cajero busca la venta original en el sistema y procesa la devolución, lo que internamente revierte la operación: el producto vuelve al inventario y el dinero se registra como una salida de la caja.